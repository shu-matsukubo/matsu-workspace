interface:
  display_name: "Handle GitHub Issue Event"
  short_description: "Safely route GitHub Issue events to existing skills"
  default_prompt: "Use $handle-github-issue-event to evaluate this GitHub Issue event and delegate the next approved action safely."
