interface:
  display_name: "Handle GitHub Issue Request"
  short_description: "Route an owner Issue request to existing skills safely"
  default_prompt: "Use $handle-github-issue-event to evaluate this owner's natural-language GitHub Issue request and delegate the next safe action."
